<%= link_to "Chatroom", root_path, class: "item" %>
<a class="item">
  Messages
</a>
<div class="right menu">
  <div class="item">
    <% if logged_in? %>
      <%= button_to 'Logout', logout_path, method: :delete, class: "ui primary button" %>
  <% else %>
      <%= link_to "Log in", login_path, class: "ui primary button" %>
    <% end %>
  </div>
</div>